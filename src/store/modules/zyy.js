const zyy = {
    state: {
      localTalkData: JSON.parse(window.localStorage.getItem(window.sessionStorage.getItem('uid')))
//        属性
    },
    mutations: {
//      方法
    }
};

export default zyy;
